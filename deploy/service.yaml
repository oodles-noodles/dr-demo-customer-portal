apiVersion: v1
kind: Service
metadata:
  name: dr-demo-customer-portal
  namespace: customer
spec:
  type: ClusterIP
  selector:
    app: dr-demo-customer-portal
  ports:
    - port: 80
      targetPort: 8080
